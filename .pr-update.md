## Summary

Completes V2 Phase 2 transition so board editing no longer depends on Excalidraw.

Closes #8

## Completed

- **Focused board route**: Added `/canvas/$id/board/$boardId` route comparable to the prototype route
- **BoardStudioPage inside V2 shell**: Uses ProjectShell (not custom header), extracts resources, shows board in sidebar
- **Truly read-only board view**: Uses KanbanBoard in preview mode - no misleading editable UI
- **Open Board affordance**: Canvas kanban preview has "Open Board" button that navigates to focused view
- **Resource navigation**: 
  - CanvasContainerV2 extracts board and prototype elements from canvas
  - ProjectShell displays dynamic resources in sidebar
  - Clicking a board in sidebar navigates to focused board view
- **Canvas-hosted board behavior**: Kanban boards on canvas render in preview mode only
- **Prototype behavior**: Continues to work as before

## Intentionally Incomplete

- **Board editing is temporarily unavailable** in the focused view
  - Board view shows read-only preview (honest behavior)
  - No fake-disabled save button or editable UI that doesn't persist
  - Full write path will be restored in a later phase

## Verification

- TypeScript: `bun run typecheck` ✅
- Tests: `bun run test --filter=@ai-canvas/web` (605 tests) ✅
- Manual verification path:
  1. Open canvas with kanban boards
  2. See boards listed in sidebar under Resources
  3. Click board in sidebar → navigates to focused board view
  4. OR click "Open Board" button on canvas kanban preview
  5. Board shows read-only preview mode
  6. Shell navigation provides path back to canvas

## Changes Made

- `apps/web/src/routes/_app/canvas.$id.board.$boardId.tsx` - New board route
- `apps/web/src/components/board/BoardStudioPage.tsx` - Focused board view using ProjectShell
- `apps/web/src/components/overlays/kanban/KanbanBoard.tsx` - Added onOpenBoard prop and button
- `apps/web/src/components/overlays/kanban/kanban-board-types.ts` - Added onOpenBoard prop type
- `apps/web/src/components/canvas/overlay-definition-types.ts` - Added onOpenBoard to OverlayRenderProps
- `apps/web/src/components/canvas/overlay-definitions.tsx` - Pass onOpenBoard to KanbanBoard
- `apps/web/src/components/canvas/CanvasNotesLayer.tsx` - Provide navigation handler to kanban overlays
- `apps/web/src/components/canvas/CanvasContainerV2.tsx` - Resource extraction and navigation
- `apps/web/src/components/shell/ProjectShell.tsx` - Dynamic resource support
- `apps/web/src/routes/_app/canvas.$id.tsx` - Handle board sub-route
